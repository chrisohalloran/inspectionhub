{
  "name": "@inspection/delivery",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "development": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "clean": "rimraf dist",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@inspection/approvals": "workspace:*",
    "@inspection/contracts": "workspace:*",
    "@inspection/domain": "workspace:*",
    "@inspection/reporting": "workspace:*",
    "@inspection/security": "workspace:*"
  },
  "devDependencies": {
    "@inspection/test-fixtures": "workspace:*"
  }
}
